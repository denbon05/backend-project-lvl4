extends ../layouts/application.pug

block header
  = t('views.statuses.titleUpdate')

block content
  +formFor()(action=route('updateStatus', { id: status.id }) method='post')
    +input(status, 'name', errors)(type="text")
    +submit()(value=t('views.manage.edit'))
    +buttonTo(route('updateStatus', { id: status.id }), 'patch')
