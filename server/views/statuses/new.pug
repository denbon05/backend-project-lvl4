extends ../layouts/application.pug

block header
  = t('views.statuses.new.title')

block content
  +formFor()(action=route('statusCreate') method='post')
    +input(status, 'name', errors, false)(type="text")
    +submit(status)(value=t('views.manage.submit'))
