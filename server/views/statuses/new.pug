extends ../layouts/application.pug

block header
  = t('views.statuses.new.title')

block content
  +formFor()(action=route('statuses') method='post')= t('views.statuses.name')
    +input(status, 'name', errors, false)(type="text")
    +submit(status)(value=t('views.statuses.new.submit'))
